import 'package:flutter/material.dart';

class FligthPage extends StatelessWidget {
  const FligthPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('itinerarios pasajes'),
    );
  }
}